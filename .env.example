# Prefer npm Trusted Publishing from GitHub Actions instead of a long-lived token:
# https://docs.npmjs.com/trusted-publishers
#
# Local token fallback only, when Trusted Publishing is not available.
# Use a short-lived, package-scoped granular token and revoke it after publishing.
# Create at https://www.npmjs.com/settings/tokens
NPM_TOKEN=
