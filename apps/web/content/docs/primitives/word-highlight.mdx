---
title: Word Highlight
description: Spring-animated highlighter wipe on a word in a sentence.
---

import { WordHighlightPreview } from '@/components/previews/word-highlight';

<InstallCommand name="word-highlight" />

<RemotionPreview component={WordHighlightPreview} durationInFrames={60} />

Finds `highlightWord` in `text` and wipes a highlight behind it using `spring()` — the pattern from Remotion's text-animation guidance. Uses `springSmooth` from `springs.ts`.
